using System.Windows.Controls;

namespace JsonDataDocuments.Pages;

public partial class FriendSearch : Page
{
    public FriendSearch() => InitializeComponent();
}
// TODO >> Name input, submit btn, list of search results > details
///    MISC. >> will need maxid method from api, names must be unique
///     which has to be enforced in the UI